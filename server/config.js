require("dotenv").config();

module.exports = {
    DB_USER,
    DB_PASSWORD,
    DB_HOST,
    DB_NAME,
    TOKEN,
    // GOOGLE_CLIENT_ID, 
    // GOOGLE_CLIENT_SECRET,
    // DB_DEPLOY,
    PORT
} = process.env;